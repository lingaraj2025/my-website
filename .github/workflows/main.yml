name: CI with Reusable Python Workflow
on: push
jobs:
  ci:
    uses: <your-username>/custom-reusable-workflow/.github/workflows/ci-python.yml@production
    with:
      python-version: '3.9'
      coverage: true
